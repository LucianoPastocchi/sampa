package com.sampa.repository;

import com.sampa.entity.TransitHome;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransitHomeRepository extends JpaRepository<TransitHome, Long> {
}